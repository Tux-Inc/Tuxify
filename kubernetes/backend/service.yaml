apiVersion: v1
kind: Service
metadata:
  name: tuxify-api-gateway
  labels:
    app: tuxify-api-gateway
spec:
  selector:
    app: tuxify-api-gateway
  ports:
    - name: tuxify-api-gateway
      port: 80
      targetPort: 3000
